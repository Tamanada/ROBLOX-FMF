--!strict
-- server/Services/FullMoonService.luau — horloge UTC globale (os.time()
-- modulo 7200 s, §10.5), multiplicateurs, Moon Shards.
-- Blueprint §2.3, §10.2 — implemented in Sprint 3 (Roadmap §12).
-- Sprint 0: structural stub only. No gameplay logic before S3.

local FullMoonService = {}

function FullMoonService.Init() end

return FullMoonService
